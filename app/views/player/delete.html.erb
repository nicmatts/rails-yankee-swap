<div class="banner"><h2 class="banner"><a href="/" class="home">Mattson Gift Game</a></h2></div>

<div class="container">
  <div class="col-md-4 col-md-offset-4 player new">
    
    <h2>Are you sure you want to delete <%= @player.name %>?</h2>
    <%= form_for(:player, :url => {:action => 'destroy', :id => @player.id}) do |f| %>

    <div class="form buttons">
      <%= submit_tag("Delete player", :class => 'btn') %>
    </div>

    <% end %>

   <%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>
   </div>
 </div>
</div>