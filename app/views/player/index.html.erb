<h1>Players</h1>

<table>
  <% @players.each do |player| %>
    <tr>
      <td><%= link_to(player.name, {:action => 'edit', :id => player.id}, :class => 'back-link') %></td>
    </tr>
  <% end %>
</table>