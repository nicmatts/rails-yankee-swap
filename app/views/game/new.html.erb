<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class = "game new">
  <h2>Add Game</h2>

  <%= form_for(:game, :url => {:action => 'create'}) do |f| %>

  <%= render(:partial => "form", :locals => {:f => f}) %>

  <div class="form buttons">
    <%= submit_tag("Add new game", :class => 'btn') %>
  </div>

  <% end %>
</div>
